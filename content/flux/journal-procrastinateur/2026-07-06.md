---
title: "Jour 105 - Nettoyer, mesurer, aligner"
date: 2026-07-06
description: "Jour 105 du journal : CI, refactors frontend/backend, fine art, observabilité WooCommerce et alignement multi-blocs jusqu'à la release 0.10.8."
layout: "procrastinateur-single"
type: "journal-procrastinateur"
draft: false
source_path: "docs/blog/2026-07-06.md"
tags: ["keredit", "editor", "ci", "observability", "woocommerce", "release"]
---
## Horaires de session


- Agent utilisé : **Codex**
- Environnement principal : **VS Code WSL, plugin Codex, repo local, VPS krampouz pour vérification runtime**
- Date couverte : **lundi 6 juillet 2026**, avec séquence prolongee après minuit en conversation / rattrapage
- Branche KEREDIT observée : **`dev`**
- SHA local `dev` / `origin/dev` observe : **`3bbc1038`** (`Merge pull request #180 from kerooblik/codex/editor-alignment-v2`)
- SHA VPS observe pendant la vérification : **`850ce648`** (`chore(release): v0.10.8`)
- Version KEREDIT observée : **0.10.8**
- Temps minimal observe le 6 juillet : **12h00+ d'amplitude de commits** (`11:37` -> `23:37`, hors conversation, alerting, deploy et rattrapage après minuit)
- Temps cumulé minimal depuis le début du journal : **environ 640h50+** (`628h50+` au 5 juillet, + `12h00+`)

## Lecture courte

Grosse journée de consolidation. Elle ne tient pas en un seul chantier : il y a
de la CI, de la dette retiree, de la performance admin, du fine art, du load
test Google Photos, de l'observabilité WooCommerce et, en fin de soirée, un gros
bloc sur le snapping et l'alignement multi-blocs de l'éditeur.

La matinée verrouille les controles :

- `4f319c6f` aligne les versions documentees ;
- `0cb6cbd8` rattache les tests frontend orphelins à la CI ;
- `18bd2cc1` lance les checks CI sur les pushes `dev`.

Le début d'après-midi nettoie et isole :

- `4f219464` aligne la validation de taille upload ;
- `f7c2bb5c` retire le service PDF local orphelin ;
- `40bc65c2` stabilise `knip` et les dépendances QR ;
- `eeb33022` extrait les commandes partagees des éléments image.

Ensuite viennent les chantiers de structure :

- `464997d8` pagine les listes admin en base ;
- `3f6f9e01` isole les hooks de l'`EditorCanvas` ;
- `23d4a626` autorise l'overflow image livre sous les couvertures intérieures ;
- `44bbec47` extrait le modèle de mutation fine art ;
- `3e766d15` ajoute un scénario k6 pour la bibliothèque Google Photos ;
- `a6c290dd` préserve les pages fine art courantes.

La partie ops corrige aussi le SHA de build frontend : `122c0d99` l'injecte
dans le build, puis `6b6c1fa6` évite le SHA vide. La journée passe par plusieurs
releases (`0.10.4`, `0.10.5`, `0.10.6`, puis `0.10.8`).

## Incident observabilité WooCommerce

L'alerte Grafana "Erreurs 5xx > 2%" n'a pas revele une panne générale. Les
probes VPS montraient un backend sain, et les compteurs concentraient les 500
sur `POST /api/woocommerce/webhook`.

Le diagnostic a remonte au webhook WooCommerce `13`, actif sur `ooblik.com`,
topic `order.updated`, URL KEREDIT `woo_ooblik_live`. Ce webhook est légitime :
il relie les commandes WooCommerce a KEREDIT. Le bruit vient du ping natif
WooCommerce `deliver_ping()`, qui envoie un corps non JSON du type
`webhook_id=13`. KEREDIT attend un JSON de commande signe et répondait donc en
500 a ce ping. Les vrais webhooks de commande continuaient a passer.

Conclusion opérationnelle : pas d'incident backend global, mais une correction
KEREDIT reste utile pour absorber ce ping en `200` ou en rejet contrôle, sans
polluer les alertes 5xx.

## Fin de soirée : aligner les blocs

La fin de journée revient au cœur de l'éditeur :

- `31c6a791` stabilise le snapping frontend ;
- `6f53c521` ajoute l'alignement multi-blocs ;
- `0e1a4d48` ajoute les outils avances d'alignement ;
- `57fbe183` aligne les guides de resize sur les bords de blocs ;
- `72af06db` garde les outils visibles en multi-sélection ;
- `b1897f24` ferme le sujet lint ;
- `3bbc1038` fusionne la PR d'alignement dans `dev`.

Le VPS observe en fin de vérification servait `0.10.8` sur `850ce648`. Le local
et `origin/dev` étaient déjà un cran plus loin avec la merge PR `3bbc1038`.

## Pour la suite

Deux fils restent a garder en tête :

- corriger proprement le ping WooCommerce pour sortir ce bruit des alertes 5xx ;
- vérifier en vrai dans l'éditeur que snapping, guides et alignement multi-blocs
  restent ergonomiques, pas seulement couverts par tests.
