---
title: "Jour 92 - Du partage public au livrable web"
date: 2026-06-22
description: "Journée de consolidation KEREDIT : couverture livre, lecteur public, PDF web téléchargeable, roadmap septembre, audit dépendances et vente WooCommerce du livrable PDF web."
layout: "procrastinateur-single"
type: "journal-procrastinateur"
draft: true
source_path: "docs/blog/2026-06-22.md"
tags: ["keredit", "partage", "book", "gof", "pdf-web", "woocommerce", "dépendances", "roadmap", "bêta"]
---
## Horaires de session


- Agent utilisé : **Codex**
- Environnement principal : **VS Code WSL, plugin Codex à gauche, code au centre, terminal Codex à droite**
- Date : **lundi 22 juin 2026**
- Branche KEREDIT : **`dev`**
- Version KEREDIT observée dans le repo : **0.9.15**
- État `dev` / `origin/dev` observe avant le commit de clôture : `dd7aae0` (`docs(partage): aligner le contrat GOF et PDF web`)
- Dernier merge GitHub du jour vérifie : PR `#89`, merge commit `5372e19429ae88b58399979f732c6c33b55368cf`
- Fenêtre de travail observée : **09:06 -> 19:50**
- Temps minimal observe aujourd'hui : **environ 10h44 d'amplitude**
- Temps cumulé minimal depuis le début du journal : **environ 536h27+**

## Commits principaux

Sur le premier-parent de `dev` :

- `fb88c2c` - `fix(book): separer couvertures non rendues du cahier`
- `868a23b` - `chore: bump release to 0.9.12`
- `5372e19` - `chore: update dependency toolchain (#89)`
- `5b11b22` - `Merge web PDF delivery controls`
- `0686895` - `fix(partage): proxy PDF web downloads`
- `b482cc2` - `chore(release): bump to 0.9.14`
- `d7d2cfc` - `fix(partage): resolve GOF artifact download origin`
- `dd7aae0` - `docs(partage): aligner le contrat GOF et PDF web`

Sur les branches integrees ou fermees autour de la journée :

- `1278567` - `fix(book): align share cover rendering contract`
- `977b6e4` - `feat: offer web PDF delivery per order`
- PR `#89` - résolution groupée des issues `#76`, `#77`, `#78`, `#79`

## Le fil de la journée

La veille avait rendu le partage public robuste. Aujourd'hui, le travail bascule
vers une question plus commerciale : comment faire du partage un vrai livrable
client, sans redonner au navigateur des URLs internes, sans confondre PDF web et
PDF atelier, et sans rouvrir tous les risques de la forge PDF dans KEREDIT.

Le thème dominant est donc la frontière :

```text
KEREDIT decide, vend, expose et protege.
GOF fabrique les artefacts optimises.
WooCommerce porte le panier et le paiement.
```

Cette phrase devient moins théorique qu'hier. Elle se retrouve dans le lecteur
public, dans le contrat livre, dans le toggle admin `PDF web`, dans la
configuration GOF, puis dans le chainon WooCommerce qui permet d'acheter le PDF
web après une commande déjà passée.

## Livre : séparer couverture atelier et lecture web

Le premier commit de la journée, `fb88c2c`, corrige une ambiguïté importante sur
les produits livre / zine. Une couverture atelier a plat n'est pas une page de
lecture. Pour l'atelier, la couverture reste un support technique. Pour le
lecteur public, il faut présenter un objet lisible :

```text
1re couverture
page blanche
cahier
page blanche finale
4e couverture
```

Le commit `1278567` prolonge cette logique dans le partage. Les zones de
couverture sont remappees pour que la première et la quatrième de couverture
soient extraites correctement depuis la couverture exterieure a plat. Les tests
de layout livre, d'export payload et de preview encadrent ce contrat.

C'est exactement le genre de détail qui fait passer KEREDIT d'un éditeur de
pages à un outil d'atelier : une structure correcte pour produire n'est pas
toujours la structure correcte pour lire.

## Lecteur public : du polish vers le produit

Une grande partie de la journée a aussi poli le lecteur public :

- styles de navigation plus simples ;
- table des matieres visuelle ;
- miniatures mieux alignées ;
- couture centrale plus lisible ;
- protection de la dernière page du cahier ;
- respect des polices rich text dans l'aperçu public ;
- modale de partage dans l'éditeur ;
- action de copie du lien mieux alignée ;
- toggle plus clair pour l'état de partage.

Ces commits ne sont pas tous sur le premier-parent final, mais ils expliquent le
merge `5b11b22`. Le lecteur n'est plus seulement une preuve technique de
StPageFlip. Il devient une surface client : navigable, partageable, moins
fragile, plus proche de l'objet imprime.

## PDF web : vendre un livrable sans exposer GOF

Le gros saut fonctionnel du jour est le PDF web.

`5b11b22` introduit les controles de livraison PDF web :

- toggle `PDF web` dans `Admin > Commandes` ;
- champ `ProjectShare.pdf_web_enabled` pour contrôler l'accès public ;
- génération GOF `web_share` capable de porter un artefact `web_preview_pdf` ;
- bouton de téléchargement dans le lecteur public quand le droit est actif ;
- tests backend et frontend autour du partage et du preview.

`0686895` corrige ensuite un point de sécurité et d'architecture : le navigateur
client ne doit jamais recevoir l'URL GOF brute. Le téléchargement passe donc par
KEREDIT :

```text
/api/project-shares/:token/artifacts/web-pdf/download
```

KEREDIT vérifie le token, l'état du partage et le droit PDF web, puis recupere
l'artefact GOF cote serveur et renvoie le PDF au client.

`d7d2cfc` corrige enfin l'origine des artefacts GOF : si un ancien artefact garde
une URL LAN, KEREDIT sait la réécrire vers l'origine publique configuree avant
de la télécharger. C'est un détail très concret, mais essentiel pour éviter le
classique bug "ça marche depuis le réseau local, pas depuis le VPS".

## Roadmap : septembre remplace la bêta comme contrainte dure

Le commit `a09ebbc`, integre dans le merge du jour, ajoute
`docs/produit/roadmap-operationnelle-septembre-2026.md`.

La contrainte y est beaucoup plus nette que les anciennes notes bêta :

> le 30 septembre 2026, le contrat commercial Printbox doit pouvoir s'arrêter.

La bêta ne disparait pas. Elle change de rôle. Elle n'est plus le but final, elle
devient l'outil de réduction de risque avant migration commerciale.

La roadmap remet le sujet dans le bon ordre :

1. vendre et reprendre des projets sur `ooblik.com` ;
2. retrouver commandes, clients, fichiers, options et support ;
3. distinguer produits autonomes et produits accompagnes ;
4. préparer l'import clients Printbox ;
5. ne pas promettre la migration éditable des anciens projets tant qu'elle n'est
   pas prouvée.

Cette clarification compte parce que la technique du jour, PDF web compris, ne
sert pas a faire une belle démo. Elle sert à remplacer progressivement une
dépendance commerciale réelle.

## Dépendances : résoudre les issues #76 a #79 sans tout rouvrir

La PR `#89` ferme le lot dépendances prepare plus tôt :

- `#76` : elimination de l'advisory `esbuild` résiduel via migration Vite 8 ;
- `#77` : mise à jour patch/minor low-risk ;
- `#78` : caracterisation Tiptap avant migration ;
- `#79` : cadrage better-auth, better-call et Zod 4.

Le merge commit `5372e19` passe la chaîne frontend sur `vite@8.0.16` et
`@vitejs/plugin-react@6.0.2`, releve le plancher Node a `>=22.12.0`, adapte
`manualChunks` a Vite 8/Rolldown, puis documente les décisions sensibles dans
`docs/audits/2026-06-22-dependances-toolchain-tiptap-auth.md`.

Le point important n'est pas "tout est à jour". Le point important est plus
discipline : ce qui pouvait être mis à jour sans gros risque l'a été ; Tiptap,
better-auth/Zod, BullMQ/ioredis, React, Prisma et Tailwind restent des chantiers
separes au lieu d'être melanges dans un lot de maintenance.

Les issues `#76`, `#77`, `#78` et `#79` ont été fermees explicitement après le
merge de la PR `#89`.

## WooCommerce : vendre le PDF web avant et après commande

La fin de journée transforme le PDF web en vrai parcours commercial, pas
seulement en override atelier.

Avant commande, le produit KEREDIT peut exposer l'option standard :

```text
livrables-web = none | pdf_web
```

Le seed ajoute cette option au carnet classique, et le flux existant
`selected_options` continue de porter le choix jusqu'au projet. Le partage garde
ensuite la même règle : GOF ne reçoit `web_preview_pdf` que si KEREDIT a bien
`livrables-web=pdf_web`.

Après commande, le plugin WordPress `keredit-connector` passe en `0.1.60` et
ajoute une option de configuration `Produit PDF web`. Dans
`Mon compte > Projets`, le bouton `Ajouter le PDF web` apparaît seulement pour
un projet déjà commande, appartenant au client connecte, et sans droit PDF web
actif.

Le panier WooCommerce reçoit alors un produit complémentaire avec deux metas
cachees :

```text
_keredit_project_id
_keredit_addon_type = pdf_web
```

Quand WooCommerce notifie KEREDIT après paiement, le backend vérifie le tenant,
le projet, le fait que le projet a déjà une commande, et l'email du client. Si
tout concorde, KEREDIT active `Project.options["livrables-web"] = "pdf_web"`,
met à jour `ProjectShare.pdf_web_enabled` si un partage existe déjà, puis
redemande un rendu GOF `web_share`.

Comme ce code touche le plugin WordPress, il devra être traite comme une vraie
release plugin. La partie code est préparée avec :

- header plugin et constante `KEREDIT_CONNECTOR_VERSION` en `0.1.60` ;
- changelog `readme.txt` ;
- `manifest.example.json` aligne sur `0.1.60` ;
- ZIP local `keredit-connector-0.1.60.zip` genere ;
- checksum manifeste `sha256:5131ba68bcca8df62aeb54444756c0beb2ff2676de54fc6484b1f63788e48136`.

La publication effective sur `updates.ooblik.com` reste à faire au moment du
deploy nominal, via le workflow plugin complet.

## État en fin de billet

- `dev` et `origin/dev` pointent sur `dd7aae0` avant le commit de clôture de ce chantier.
- La version package locale observée est **0.9.15**.
- Le plugin WordPress est prepare en **0.1.60** pour l'achat post-commande du PDF web.
- La PR `#89` est mergee dans `dev`.
- Les issues `#76`, `#77`, `#78` et `#79` sont fermees.
- Le billet du 21 juin a été relu et corrige : son dernier commit livre était
  `34376b4`, pas `170b6cb`.
- Le commit de clôture doit embarquer le chantier PDF web avant/après commande,
  le bump plugin et cette mise à jour de billet.

## Suite logique

La suite courte :

1. configurer cote WooCommerce le produit simple `PDF web KEREDIT` et reporter
   son ID dans l'intégration KEREDIT ;
2. publier la release plugin `0.1.60` avec le workflow complet ;
3. rejouer le parcours complet `Mon compte > Projets -> Ajouter le PDF web ->
   panier Woo -> webhook -> partage public -> téléchargement KEREDIT` ;
4. garder la roadmap septembre comme filtre : chaque chantier doit aider la
   bascule OOBLIK hors Printbox, ou rester explicitement accompagne.

## Vérification

Traces utilisées pour ce billet :

- `git status --short --branch`
- `git log --since='2026-06-21 00:00' --until='2026-06-21 23:59:59' --first-parent dev`
- `git log --since='2026-06-22 00:00' --until='2026-06-22 23:59:59' --first-parent dev`
- `git log --since='2026-06-21 00:00' --until='2026-06-22 23:59:59' --all`
- `git show --stat` sur les commits principaux
- `gh pr list --state merged`
- `gh issue list --state closed --search "closed:2026-06-22"`
- lecture de `docs/architecture/partage-public.md`
- lecture de `docs/guides/gof-flux-configuration-client.md`
- lecture de `docs/produit/roadmap-operationnelle-septembre-2026.md`
- lecture puis finalisation du diff local WooCommerce / PDF web
- `pnpm plugin:lint`
- `pnpm plugin:build`
- `node .codex-skills/wp-plugin-release/scripts/prepare-manifest.mjs`
- `pnpm typecheck`
- `pnpm --filter frontend lint`
- `pnpm --filter frontend test:project-share-reader`
- tests backend cibles `project share PDF web entitlement` et `WooCommerce paid PDF web addon`

Limite de vérification : `pnpm --filter backend test -- woocommerce
project-share-service` lance ici une suite backend large. Le nouveau scénario
Woo PDF web passe, mais la commande globale echoue sur un test intégration photo
upload existant et sans lien direct avec ce chantier.
